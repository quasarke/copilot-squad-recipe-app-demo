# Project Context

- **Owner:** Zack Way
- **Project:** RecipeHub — a full-stack recipe management app built with .NET Aspire 13.2. .NET 10 Minimal API backend with EF Core 10 + SQLite. React 19 + TypeScript + Vite 6 frontend with TanStack Query v5. Aspire dashboard for observability. Built for the GitHub Copilot & Squad Developer Workflow Hackathon.
- **Stack:** .NET 10, Aspire 13.2, EF Core 10, SQLite, React 19, TypeScript, Vite 6, TanStack Query v5, xUnit
- **Created:** 2026-05-27

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->
- `tests\RecipeHub.Api.Tests\TestBase.cs` provides `RecipeApiFactory`, a shared `WebApplicationFactory<Program>` that uses one temp-file SQLite database per test class, applies migrations, and boots the app with seeded data.
- `src\RecipeHub.Api\Data\SeedData.cs` seeds 12 sample recipes with stable IDs starting at 1 and leaves `Favorites` empty, which makes contract-first favorites integration tests straightforward.
- When API tests share the same factory-backed database, use unique `userId` values per test to avoid cross-test state bleed while still using `IClassFixture<RecipeApiFactory>`.
- Favorites integration tests (`tests/RecipeHub.Api.Tests/FavoriteEndpointTests.cs`) use contract-first approach with private test-only DTOs, allowing QA to land full coverage (12 tests: CRUD flow, 404, 409 duplicate, per-user isolation, default-user) before backend DTO shape is finalized.
- 2026-05-27: Husky is rooted in the repo-level `package.json`; `pre-commit` runs `npm run verify:precommit`, which covers frontend lint/tests and `dotnet test`.
- 2026-05-27: Git hooks are unified at repo root to cover both .NET and frontend validations in one pre-commit gate. No new tools added; orchestrates existing npm and dotnet commands.
- 2026-05-27: Cook Mode accessibility audit (issue #1) — key files: `src/RecipeHub.Web/src/pages/CookModePage.tsx` + `.module.css`, `src/hooks/useCookMode.ts`, `src/components/ui/Button.tsx`. Current implementation has solid semantic foundation (landmarks, ARIA live regions) but fails on: contrast (back link `#4a7fbf`, error text `#a33`), touch targets (small buttons < 24×24px), focus styles (back link missing `:focus-visible`), and screen reader announcements (step changes, timer state transitions). Critical checklist items documented in `.squad/decisions/inbox/kyle-cook-mode-a11y-audit.md` for Kenny's implementation and later review gate.
- 2026-05-27: Frontend accessibility testing skill created at `.squad/skills/frontend-accessibility-testing/SKILL.md` — covers WCAG 2.2 Level AA patterns for contrast, touch targets, focus management, ARIA live regions, semantic HTML, and error states. Includes testing checklist template and automated testing integration guidance (vitest-axe).
- 2026-05-27: PR #5 review gate passed (8/8 critical items) — Kenny's initial implementation passed 5/8 (contrast, focus-visible, error alerts, button touch targets), blocked on STEP-1 (step heading announcements) and TGT-1 (back link touch target). Cartman fixed both blockers in commit `16c3c0e`: added `aria-live="polite"` to step heading (line 74) and `padding: 0.25rem 0` + `min-height: 24px` to `.back` link (lines 13-14). All WCAG 2.2 Level AA requirements met for Cook Mode accessibility. PR #5 approved for merge, ready to close issue #1.
- 2026-05-27: **Systemic WCAG 2.2 Acceptance Gate created** for issue #1 — PR #5 was too narrow (Cook Mode only). User feedback: "UI Accessibility is still broken, so many buttons that can't be read and a lot of text with bad visibility." Full app audit documented in `.squad/decisions/inbox/kyle-systemic-wcag22-gate.md`. **Critical failures (9 must-pass):** (1) disabled button text contrast fails at ~3.8:1 with `opacity: 0.65` (`Button.module.css` line 15-18), (2) ghost button on varied backgrounds borderline, (3) error text `#a33` barely passes at 4.6:1 (needs 5.0:1 margin, 6 files affected), (4) nav links missing `:focus-visible` (`App.module.css` lines 24-38), (5) search clear button <24×24px + no focus style (`SearchBar.module.css` lines 17-33), (6) filter chip focus outline `#4a7fbf` fails 3:1 non-text contrast (`FilterPanel.module.css` line 17), (7) form validation errors lack `aria-invalid` + `aria-describedby` (`RecipeEditPage.tsx` lines 192-377), (8) loading states verified correct — `Spinner.tsx` has `role="status"` + `.srOnly` label ✓ (lines 15-18), (9) disabled button states already use `aria-busy` ✓. **Important (5 should-pass):** touch targets (small buttons, nav links), card hover border contrast, dark mode decision (`color-scheme: light dark` in `index.css` with no styles), empty/error state colors. **Key files:** `Button.module.css`, `App.module.css`, `SearchBar.module.css`, `FilterPanel.module.css`, `RecipeEditPage.tsx`, 6 files with `#a33` error color. Gate requires all 9 Critical + 4/5 Important to pass. Testing protocol: manual contrast checks (WebAIM), keyboard nav (full tab-through), screen reader (NVDA/VoiceOver on 2 pages), touch target measurements (DevTools), responsive mobile (390×844px). Approval: Kyle + Kenny required, Cartman arbitrates disputes.
- 2026-05-27: Added accessibility regression tests for Button component (`src/RecipeHub.Web/src/components/ui/__tests__/Button.a11y.test.tsx`) — 10 tests covering semantic HTML, keyboard focus, disabled/loading states, ARIA attributes (`aria-busy`, `aria-hidden`), and accessible name preservation. Uses existing Vitest + `@testing-library/react`, no new tools installed. All tests pass ✓. Includes manual testing checklist for contrast (CRIT-1), focus-visible (CRIT-9), and touch targets (IMP-1) that require visual verification. Tests will catch regressions in ARIA and semantic structure; visual/contrast issues still require manual checks or future `vitest-axe` integration (coordinate with Cartman if adding new tool).
- 2026-05-27: **BLOCKING REGRESSION FOUND: Cook Mode white-on-white text failure (REGR-1).** Screenshot `copilot-image-1692f6.png` shows instruction text `Whisk flour, sugar, baking powder, baking soda, and salt in a large bowl.` is **completely unreadable** — white or near-white text on white/light gray card. Page has dark backdrop (#1a1a1a), step heading is white (readable), but instruction card (`.instruction` line 38-47 `CookModePage.module.css`) has light background (`#f5f5f5`) with white text inherited from page context. **PR #5 approval was wrong** — static CSS inspection showed correct values but rendered page fails catastrophically. Root cause: browser forced-colors, OS dark mode, or CSS variable resolution failure causes text to inherit white color while card keeps light background. **Updated WCAG gate:** Added REGR-1 as BLOCKING item. Mandated visual verification (Playwright or browser testing) in light/dark/forced-colors modes. No PR can be approved without Cook Mode visual check. Contrast measurements must use rendered values (not CSS source). Files affected: `CookModePage.module.css` (lines 38-51), `CookModePage.tsx` (lines 79-81), `index.css` (CSS variables), `App.css` (page-level inheritance). **Decision:** All UI PRs require Playwright validation evidence or local browser testing; CSS inspection alone is insufficient. Reviewers must verify visually before approval. **Fix applied:** Added explicit `color: var(--color-text-primary, #1a1a1a)` to `.instruction p` (line 51) in commit `fe673f1`. Added `CookModePage.a11y.test.tsx` with 6 passing regression guard tests.
- 2026-05-27: **MANDATORY PROCESS DIRECTIVE from Zack — CRIT-0 added to gate (Playwright validation).** All UI accessibility PRs for issue #1 MUST include Playwright or browser validation of actual rendered surfaces before approval. Static CSS inspection, lint/tests/build, and claimed contrast values are insufficient. Directive documented in `.squad/decisions/inbox/copilot-directive-2026-05-27T15-05-24-104-04-00.md`. **Updated WCAG gate with CRIT-0:** Mandatory Playwright validation requirement added. Specified 9 surfaces to inspect: (1) Cook Mode (mandatory REGR-1 blocker), (2) Recipe Detail, (3) List/Cards, (4) Home, (5) Favorites, (6) Edit Form, (7) Header/Nav, (8) Buttons (all states), (9) Loading/Error/Status. **Minimum 7/9 surfaces required, Cook Mode non-negotiable.** Playwright checklist: capture light mode + OS dark mode + forced-colors screenshots, measure rendered contrast (not CSS), verify no white-on-white text, test at multiple viewports (mobile 390×844px, tablet, desktop). **Rejection criteria:** No Playwright/browser evidence → REJECT. Cook Mode not tested → REJECT. Unreadable text in screenshots → REJECT. Static CSS inspection only → REJECT. **If Playwright cannot run:** Browser testing with screenshots is acceptable fallback, but if neither is possible, must report blocker to coordinator — do NOT approve. **Approval note format mandated:** Kyle must document Playwright evidence review in all PR approvals (CRIT-0 status, REGR-1 contrast measurements, per-surface results, decision with reason). Aspire MCP tools available to assist with running/observing app stack for Playwright testing. This directive prevents repeat of PR #5 failure where CSS looked correct but rendered UI was unreadable. Gate now has 10 Critical items (CRIT-0 through CRIT-9), 5 Important items, plus REGR-1 blocking regression.

